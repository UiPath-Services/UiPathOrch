---
external help file: UiPath.PowerShell.OrchProvider.dll-Help.xml
Module Name: UiPathOrch
online version:
schema: 2.0.0
---

# Get-OrchTestCaseExecution

## SYNOPSIS
Gets test case execution records from UiPath Orchestrator.

## SYNTAX

```
Get-OrchTestCaseExecution [[-Name] <String[]>] [-Path <String[]>] [-Recurse] [-Depth <UInt32>]
 [-ProgressAction <ActionPreference>] [<CommonParameters>]
```

## DESCRIPTION
The Get-OrchTestCaseExecution cmdlet retrieves test case execution records from UiPath Orchestrator. Test case executions represent individual runs of automated test cases, including their execution status, timing information, input/output arguments, and related metadata.

This is a folder entity operation. You must either navigate to a specific folder using Set-Location (cd) or use the -Path, -Recurse, or -Depth parameters to specify target folders.

Primary Endpoint: [PLACEHOLDER - Test case execution API endpoint]

OAuth required scopes: [PLACEHOLDER - Test execution read scope]

Required permissions: [PLACEHOLDER - Test case execution view permissions]

## EXAMPLES

### Example 1: Get test case executions from current folder
```powershell
PS C:\> cd Orch1:\root
PS Orch1:\root> Get-OrchTestCaseExecution
```

Navigates to the root folder and retrieves all test case executions from that folder.

### Example 2: Get test case executions recursively
```powershell
PS C:\> Get-OrchTestCaseExecution -Recurse
```

Retrieves all test case executions from all folders recursively, starting from the root folder.

### Example 3: Get test case executions by name pattern
```powershell
PS C:\> Get-OrchTestCaseExecution -Recurse -Name "*Main*"
```

Retrieves test case executions whose entry point path contains "Main" from all folders recursively.

### Example 4: Get test case executions from specific path
```powershell
PS C:\> Get-OrchTestCaseExecution -Path Orch1:\root\MyProject
```

Retrieves test case executions from the specified folder path.

### Example 5: Get test case executions with depth limit
```powershell
PS C:\> Get-OrchTestCaseExecution -Recurse -Depth 2
```

Retrieves test case executions recursively but limits the search to 2 levels deep from the current folder.

### Example 6: Get execution details and examine structure
```powershell
PS C:\> Get-OrchTestCaseExecution -Recurse | Select-Object Path, -First 1 | ConvertTo-Json -Depth 5
```

Retrieves the first test case execution and displays its complete object structure in JSON format for detailed analysis.

### Example 7: Filter executions by status
```powershell
PS C:\> Get-OrchTestCaseExecution -Recurse | Where-Object {$_.Status -eq "Passed"} | Select-Object Path, Id, EntryPointPath, StartTime, EndTime
```

Gets all test case executions and filters for those with "Passed" status, displaying key execution information.

### Example 8: Get recent test executions
```powershell
PS C:\> Get-OrchTestCaseExecution -Recurse | Where-Object {$_.StartTime -gt (Get-Date).AddDays(-7)} | Sort-Object StartTime -Descending
```

Gets test case executions from the last 7 days and sorts them by start time in descending order.

## PARAMETERS

### -Depth
Specifies the maximum depth of recursion when used with -Recurse. Controls how many levels deep the search will go in the folder hierarchy.

```yaml
Type: UInt32
Parameter Sets: (All)
Aliases:

Required: False
Position: Named
Default value: None
Accept pipeline input: False
Accept wildcard characters: False
```

### -Name
Specifies the name pattern for test case executions to retrieve. Supports wildcard characters (* and ?) for pattern matching. Matches against the EntryPointPath property.

```yaml
Type: String[]
Parameter Sets: (All)
Aliases:

Required: False
Position: 0
Default value: None
Accept pipeline input: True (ByPropertyName)
Accept wildcard characters: True
```

### -Path
Specifies the folder paths where to search for test case executions. Supports wildcard characters (* and ?) for pattern matching. Use this parameter when you want to target specific folders without changing the current location.

```yaml
Type: String[]
Parameter Sets: (All)
Aliases:

Required: False
Position: Named
Default value: None
Accept pipeline input: True (ByPropertyName)
Accept wildcard characters: True
```

### -Recurse
Searches for test case executions in all subfolders recursively. When specified, the cmdlet searches through the entire folder hierarchy.

```yaml
Type: SwitchParameter
Parameter Sets: (All)
Aliases:

Required: False
Position: Named
Default value: False
Accept pipeline input: False
Accept wildcard characters: False
```

### -ProgressAction
Specifies how PowerShell responds to progress updates generated by a script, cmdlet, or provider.

```yaml
Type: ActionPreference
Parameter Sets: (All)
Aliases: proga

Required: False
Position: Named
Default value: None
Accept pipeline input: False
Accept wildcard characters: False
```

### CommonParameters
This cmdlet supports the common parameters: -Debug, -ErrorAction, -ErrorVariable, -InformationAction, -InformationVariable, -OutVariable, -OutBuffer, -PipelineVariable, -Verbose, -WarningAction, and -WarningVariable. For more information, see [about_CommonParameters](http://go.microsoft.com/fwlink/?LinkID=113216).

## INPUTS

### None
## OUTPUTS

### UiPath.PowerShell.Entities.TestCaseExecution
## NOTES
- This cmdlet operates on folder entities and requires folder navigation or path specification
- For optimal performance, place -Path, -Recurse, and -Depth parameters immediately after the cmdlet name to enable proper autocompletion
- Common execution statuses include "Passed", "Failed", "Cancelled", "Pending", and others
- InputArguments and OutputArguments contain JSON-formatted test data
- Use filtering and sorting operations to analyze test execution patterns and results
- The RuntimeType property indicates the type of robot that executed the test (e.g., "TestAutomation")
- ExecutionOrder property shows the sequence of test case execution within a test set

## RELATED LINKS

[Get-OrchTestCase]()

[Get-OrchTestSet]()

[Get-OrchTestSetExecution]()

[Start-OrchTestSet]()


