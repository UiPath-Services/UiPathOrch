---
external help file: UiPath.PowerShell.OrchProvider.dll-Help.xml
Module Name: UiPathOrch
online version:
schema: 2.0.0
---

# Remove-PmGroup

## SYNOPSIS
Removes groups from UiPath Platform Management.

## SYNTAX

```
Remove-PmGroup [-GroupName] <String[]> [-Path <String[]>] [-ProgressAction <ActionPreference>] [-WhatIf]
 [-Confirm] [<CommonParameters>]
```

## DESCRIPTION
The Remove-PmGroup cmdlet removes groups from UiPath Platform Management. Groups are collections of users that can be assigned roles and permissions collectively. This cmdlet permanently removes the specified groups and their associated configurations.

The -Path parameter specifies target drives. If not specified, the current drive will be targeted.

This cmdlet accesses Platform Management APIs and works across all UiPath Orchestrator drives (Orch1:, Orch1Tm:, Orch1Du:).

Primary Endpoint: [PLACEHOLDER - Platform Management group deletion endpoint]

OAuth required scopes: [PLACEHOLDER - PM.Group.Delete or similar]

Required permissions: [PLACEHOLDER - Platform Management group delete permissions]

## EXAMPLES

### Example 1: Remove a single group
```powershell
PS C:\> Remove-PmGroup -GroupName TestGroup
```

Removes the group named "TestGroup" from Platform Management.

### Example 2: Remove multiple groups
```powershell
PS C:\> Remove-PmGroup -GroupName TestGroup1, TestGroup2, CustomGroup
```

Removes multiple groups in a single operation.

### Example 3: Remove groups using wildcards
```powershell
PS C:\> Remove-PmGroup -GroupName Test* -WhatIf
```

Shows what would happen if all groups starting with "Test" were removed.

### Example 4: Preview removal operation
```powershell
PS C:\> Remove-PmGroup -GroupName CustomGroup -WhatIf
```

Shows what would happen if the removal operation were executed without actually removing the group.

### Example 5: Remove with confirmation prompt
```powershell
PS C:\> Remove-PmGroup -GroupName CustomGroup -Confirm
```

Removes the group with an additional confirmation prompt before proceeding.

### Example 6: Remove groups from specific drives
```powershell
PS C:\> Remove-PmGroup -GroupName TestGroup -Path Orch1:, Orch2:
```

Removes the group from multiple specified Orchestrator drives.

### Example 7: Remove empty groups
```powershell
PS C:\> Get-PmGroup | Where-Object {$_.members.Count -eq 0} | Remove-PmGroup -WhatIf
```

Gets all groups with no members and shows what would happen if they were removed.

## PARAMETERS

### -Confirm
Prompts you for confirmation before running the cmdlet. This provides an additional safety check before removing groups.

```yaml
Type: SwitchParameter
Parameter Sets: (All)
Aliases: cf

Required: False
Position: Named
Default value: None
Accept pipeline input: False
Accept wildcard characters: False
```

### -GroupName
Specifies the names of groups to remove. Supports wildcard characters (* and ?) for pattern matching. Multiple group names can be specified as an array.

```yaml
Type: String[]
Parameter Sets: (All)
Aliases:

Required: True
Position: 0
Default value: None
Accept pipeline input: True (ByPropertyName)
Accept wildcard characters: True
```

### -Path
Specifies the name of the target drives. If not specified, the current drive will be targeted. Platform Management APIs work across all Orchestrator drives.

```yaml
Type: String[]
Parameter Sets: (All)
Aliases:

Required: False
Position: Named
Default value: None
Accept pipeline input: True (ByPropertyName)
Accept wildcard characters: False
```

### -WhatIf
Shows what would happen if the cmdlet runs without actually executing the removal operation. This is useful for verifying which groups would be affected before making changes.

```yaml
Type: SwitchParameter
Parameter Sets: (All)
Aliases: wi

Required: False
Position: Named
Default value: None
Accept pipeline input: False
Accept wildcard characters: False
```

### -ProgressAction
Specifies how PowerShell responds to progress updates generated by a script, cmdlet, or provider.

```yaml
Type: ActionPreference
Parameter Sets: (All)
Aliases: proga

Required: False
Position: Named
Default value: None
Accept pipeline input: False
Accept wildcard characters: False
```

### CommonParameters
This cmdlet supports the common parameters: -Debug, -ErrorAction, -ErrorVariable, -InformationAction, -InformationVariable, -OutVariable, -OutBuffer, -PipelineVariable, -Verbose, -WarningAction, and -WarningVariable. For more information, see [about_CommonParameters](http://go.microsoft.com/fwlink/?LinkID=113216).

## INPUTS

### System.String[]
## OUTPUTS

### System.Object
## NOTES
- This cmdlet permanently removes groups and cannot be undone
- Use -WhatIf parameter to preview the removal operation before executing
- Removing a group will affect any users, roles, and permissions associated with it
- The -GroupName parameter supports wildcard characters for bulk operations
- Platform Management cmdlets (starting with "Pm") provide cross-organizational group management
- Always verify group dependencies and member assignments before removal
- Built-in system groups (like "Everyone") may be protected from deletion

## RELATED LINKS

[Get-PmGroup]()

[New-PmGroup]()

[Copy-PmGroup]()

[Add-PmGroupMember]()

[Remove-PmGroupMember]()
