---
external help file: UiPath.PowerShell.OrchProvider.dll-Help.xml
Module Name: UiPathOrch
online version:
schema: 2.0.0
---

# Resolve-PmDirectoryNameBulk

## SYNOPSIS
Resolves directory entity names to their detailed information in UiPath Platform Management.

## SYNTAX

```
Resolve-PmDirectoryNameBulk [-EntityType] <String> [-Name] <String[]> [-Path <String[]>]
 [-ProgressAction <ActionPreference>] [<CommonParameters>]
```

## DESCRIPTION
The Resolve-PmDirectoryNameBulk cmdlet resolves directory entity names (users, groups, or applications) to their detailed directory information in UiPath Platform Management. This cmdlet is useful for bulk resolution of multiple entity names to retrieve comprehensive directory details including identifiers, display names, organizational information, and source details.

This cmdlet accesses Platform Management APIs and works across all UiPath Orchestrator drives (Orch1:, Orch1Tm:, Orch1Du:).

Primary Endpoint: [PLACEHOLDER - POST "/api/Directory/BulkResolveByName/{partitionGlobalId}]

OAuth required scopes: [PLACEHOLDER - Platform Management directory read scope]

Required permissions: [PLACEHOLDER - Directory read permissions]

## EXAMPLES

### Example 1: Resolve a single user by name
```powershell
PS C:\> Resolve-PmDirectoryNameBulk -EntityType User -Name user@example.com
```

Resolves the specified user name to retrieve detailed directory information.

### Example 2: Resolve multiple users by name
```powershell
PS C:\> Resolve-PmDirectoryNameBulk -EntityType User -Name user1@example.com, "user2@example.com"
```

Resolves multiple user names in a single operation to retrieve their directory information.

### Example 3: Resolve a group by name
```powershell
PS C:\> Resolve-PmDirectoryNameBulk -EntityType Group -Name Everyone
```

Resolves the specified group name to retrieve detailed group information.

### Example 4: Resolve multiple groups
```powershell
PS C:\> Resolve-PmDirectoryNameBulk -EntityType Group -Name Administrators, "Users", "Everyone"
```

Resolves multiple group names to retrieve their directory information.

### Example 5: Resolve and examine structure
```powershell
PS C:\> Resolve-PmDirectoryNameBulk -EntityType User -Name user@example.com | ConvertTo-Json -Depth 5
```

Resolves a user name and displays the complete object structure in JSON format for detailed analysis.

### Example 6: Resolve across multiple drives
```powershell
PS C:\> Resolve-PmDirectoryNameBulk -EntityType User -Name user@example.com -Path Orch1:, Orch2:
```

Resolves user names across multiple specified Orchestrator drives.

### Example 7: Filter resolved users by source
```powershell
PS C:\> Resolve-PmDirectoryNameBulk -EntityType User -Name user1@example.com, "user2@example.com" | Where-Object {$_.source -eq "aad"} | Select-Object displayName, email, source
```

Resolves multiple users and filters for those from Azure Active Directory (AAD) source.

### Example 8: Extract user identifiers for further processing
```powershell
PS C:\> $users = Resolve-PmDirectoryNameBulk -EntityType User -Name user1@example.com, "user2@example.com"
PS C:\> $users | Select-Object displayName, identifier, email
```

Resolves multiple users and extracts key identification information for further processing.

## PARAMETERS

### -EntityType
Specifies the type of directory entity to resolve. Valid values include "User", "Group", and "Application". This parameter determines the type of directory object that will be returned.

```yaml
Type: String
Parameter Sets: (All)
Aliases:

Required: True
Position: 0
Default value: None
Accept pipeline input: True (ByPropertyName)
Accept wildcard characters: False
```

### -Name
Specifies the names of directory entities to resolve. This parameter accepts an array of names for bulk resolution operations. Names should match the directory entity names exactly.

```yaml
Type: String[]
Parameter Sets: (All)
Aliases:

Required: True
Position: 1
Default value: None
Accept pipeline input: True (ByPropertyName)
Accept wildcard characters: False
```

### -Path
Specifies the name of the target drives. If not specified, the current drive will be targeted. Platform Management APIs work across all Orchestrator drives.

```yaml
Type: String[]
Parameter Sets: (All)
Aliases:

Required: False
Position: Named
Default value: None
Accept pipeline input: True (ByPropertyName)
Accept wildcard characters: False
```

### -ProgressAction
Specifies how PowerShell responds to progress updates generated by a script, cmdlet, or provider.

```yaml
Type: ActionPreference
Parameter Sets: (All)
Aliases: proga

Required: False
Position: Named
Default value: None
Accept pipeline input: False
Accept wildcard characters: False
```

### CommonParameters
This cmdlet supports the common parameters: -Debug, -ErrorAction, -ErrorVariable, -InformationAction, -InformationVariable, -OutVariable, -OutBuffer, -PipelineVariable, -Verbose, -WarningAction, and -WarningVariable. For more information, see [about_CommonParameters](http://go.microsoft.com/fwlink/?LinkID=113216).

## INPUTS

### System.String
### System.String[]
## OUTPUTS

### UiPath.PowerShell.Entities.DirectoryUser
### UiPath.PowerShell.Entities.DirectoryGroup
### UiPath.PowerShell.Entities.DirectoryApplication
## NOTES
- This cmdlet is designed for bulk resolution operations to improve performance when resolving multiple entities
- The source property indicates the directory source (e.g., "local", "aad" for Azure Active Directory)
- Entity types must be specified exactly as "User", "Group", or "Application"
- Names must match exactly with directory entity names for successful resolution
- Platform Management cmdlets (starting with "Pm") provide cross-organizational directory information
- Use this cmdlet when you need to convert entity names to their full directory representations with identifiers

## RELATED LINKS

[Search-PmDirectory]()

[Get-PmUser]()

[Get-PmGroup]()



